extends layout

block content
  if (!user)
   a(href="/login") Login
   br
   a(href="/register") Register
  if (user)
   p You are currently logged in as #{user.username}
   a(href="/resource/books") Take me to Books
   br
   a(href="/logout") Logout
  //- h1= title
  //- p Welcome to #{title}. This is the demo express server.
  //- button(type="button",class="btn btn-secondary", onclick="goFaster()") Press me for contents
  //- div(id="faster") Increase the speed
  //- a(href='http://localhost:3000/dog') Link to Search Dog Page.
  //- script. 
  //-   var mph = 20;
  //-   var change = 1;
  //-   function goFaster(){
  //-     mph = mph+change;
  //-     change = change+1;
  //-     document.getElementById('faster').innerHTML = `MPH ${mph} with change ${change}`;}
  

